package gnu.client.module;

// Ordinals are the contract with the native ImGui tab bar
// (0 Combat, 1 Player, 2 Visuals, 3 Misc, 4 Settings).
public enum Category {
    COMBAT,
    PLAYER,
    VISUALS,
    MISC,
    SETTINGS
}

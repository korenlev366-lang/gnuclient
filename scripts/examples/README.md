# Example Scripts

**Disclaimer:** None of these Grim scripts actually bypass Grim anti-cheat. They were bad experimental attempts that did not work reliably (or at all). They are kept here as reference only — do not expect them to function on Grim-protected servers.

## General API examples

| Script | Purpose |
|--------|---------|
| `disabler.java` | Per-packet outbound/inbound queue or disable — `registerMode` for Action/Direction, `packetSendPriority()` demo |

## Grim scripts (experimental / failed)

| Script | Notes |
|--------|-------|
| `grim_*.java` | See disclaimer above |
